import { createContext } from 'react'

// Ini adalah nilai default untuk konteks
const defaultValue = {} // Anda bisa menggunakan nilai default sesuai kebutuhan

export const ContextApi = createContext(defaultValue)
